<% @items.each do |item| %>
	<%= item.title%> <%= item.price%> <%= link_to "En fait j'en veux plus", join_table_carts_item_path(JoinTableCartsItem.find_by(item_id:item.id, cart_id:@cart.id).id), method: :delete %>
	</br>
<%end%>

<p> Total : <%=@cart.total_price%> </p>
<hr>
Valider votre panier : (bouton "passer commander" à faire au lieu de ce truc moche :) et il faudra qu'il ne soit pas visible si le panier n'a pas d'article)
<%= form_tag user_orders_path() do %>
  <article>
    <% if flash[:error].present? %>
      <div id="error_explanation">
        <p><%= flash[:error] %></p>
      </div>
    <% end %>
    <label class="amount">
      <span>Amount: <%= @cart.total_price %></span>
    </label>
  </article>

  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
          data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
          data-email="<%= current_user.email %>"
          data-description="Merci de renseigner les informations ci-dessous"
          data-amount="<%= @cart.total_price*100 %>"
          data-currency="eur"
          data-locale="auto"></script>
<% end %>

